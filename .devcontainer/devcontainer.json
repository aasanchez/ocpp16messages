
{
	"name": "GoLang Base Development",
	"build": {
    "dockerfile": "Dockerfile",
    "context": ".devcontainer"
  },
  "remoteUser": "vscode",
  "updateRemoteUserUID": true,
  "containerEnv": {
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}"
  }
}
